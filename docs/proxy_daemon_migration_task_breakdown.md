# План задач для переписывания proxy-daemon.pl на Python

Документ описывает разбивку на задачи для команды разработки в продолжение
архитектурного плана из `proxy_daemon_python_migration.md`. Каждый эпик можно
реализовывать параллельно с учётом зависимостей, указанных в пункте
"Подготовка".

## Как пользоваться документом

Отмечайте чекбокс задачи по мере выполнения и оставляйте краткий комментарий
о проделанной работе в строке `Комментарий`. Комментарий может содержать ссылки
на пул-реквесты, коммиты или основные итоги.

## Эпик 0. Подготовка
- [x] **Задача 0.1. Актуализировать репозиторий и процессы**
  - Комментарий: Создан каркас пакета `scripts/proxy_daemon_py` с модулями и требованиями.
  - Создать python-подкаталог `scripts/proxy_daemon_py/`.
  - Добавить базовую структуру: `__init__.py`, `config.py`, `db.py`, `daemon.py`, `heartbeat.py`, `balancer.py`, `cli.py`.
  - Настроить общие требования (`requirements.txt`) для демона.
- [x] **Задача 0.2. Подготовить окружение и CI**
  - Комментарий: Добавлена конфигурация Poetry, настроены lint/typing-проверки и CI workflow на GitHub Actions.
  - Добавить `poetry`/`pip-tools` конфигурацию (по выбранному инструменту).
  - Настроить линтеры `black`, `ruff`, `mypy` (или эквивалент) для каталога.
  - Добавить в CI запуск линтеров и юнит-тестов.

## Эпик 1. Конфигурация и запуск
- [x] **Задача 1.1. Реализовать парсер конфигурации**
  - Комментарий: Добавлен парсер hlstats.conf с типизацией ключей и тестами на корректность разбора.
  - Совместимость с `hlstats.conf` (текущие секции и ключи).
  - Проверки: корректность путей, fallback по умолчанию.
  - Юнит-тесты на парсинг примеров.
- [x] **Задача 1.2. CLI-обёртка запуска**
  - Комментарий: Реализован парсер аргументов с опциями `--configfile`, `--debug`, `--foreground` и базовый entrypoint с тестами.
  - Опции `--configfile`, `--debug`, `--foreground`.
  - Поддержка daemonization (при необходимости) и сигналов.
  - Интеграционные тесты CLI (pytest + `subprocess`).
- [x] **Задача 1.3. Модуль логирования**
  - Комментарий: Добавлены уровни NOTICE/CONTROL/BALANCE/E403/DAEMON, конфигурация формата времени и тесты форматирования.
  - Настроить формат и уровни логов (`NOTICE`, `CONTROL`, `BALANCE`, `E403`, `DAEMON`).
  - Вынести таймзону/формат даты в конфигурацию.
  - Покрыть тестами форматирование сообщений.

## Эпик 2. Работа с БД
- [x] **Задача 2.1. Реализация адаптера БД**
  - Комментарий: Добавлен синхронный и асинхронный адаптеры с ретраями, парсингом Proxy_Daemons и тестами на мок-коннекторах.
  - Подключение к MySQL с retry (бэк-офф, таймауты).
  - Методы `fetch_options`, `fetch_proxy_key`, `fetch_daemons`.
  - Абстракция для синхронного и async-исполнения (использовать `aiomysql` или executor).
  - Тесты с моками соединения.
- [x] **Задача 2.2. Подготовка фикстур БД**
  - Комментарий: Добавлены docker-compose стенд, минимальная схема hlstats_Options и seed-данные для Proxy_Key/Proxy_Daemons.
  - SQL-скрипты с минимальными данными для локального тестирования.
  - Документация по запуску локального MySQL (docker-compose).
- [x] **Задача 2.3. Обновление состояния БД**
  - Комментарий: Реализовано сохранение heartbeat в таблицу Proxy_Daemons, добавлены тесты и фикстуры в локальном стенде.
  - Методы записи heartbeat и статусов демонов.
  - Инварианты: неизменность схемы.

## Эпик 3. Модель данных и бизнес-логика
- [x] **Задача 3.1. Классы состояния**
  - Комментарий: Реализованы `Daemon`/`DaemonManager`/`ServerAssignment`, круговой балансировщик и тесты на sticky-распределение и failover.
  - `Daemon`, `DaemonManager`, `ServerAssignment`.
  - Методы обновления статусов, таймауты, round-robin.
  - Юнит-тесты на распределение и failover.
- [x] **Задача 3.2. Импорт существующих данных**
  - Комментарий: Добавлен модуль importer с CLI для загрузки состояния из Proxy_Daemons/hlstats_Servers, проверкой рассинхронизаций и автотестами.
  - Скрипт миграции/валидации таблиц `Proxy_Daemons` и `hlstats_Servers`.
  - Сравнение текущего распределения с ожидаемым.

## Эпик 4. Сетевой слой
- [x] **Задача 4.1. Базовая реализация UDP-сервера**
  - Комментарий: Реализован asyncio UDP сервер с базовой валидацией пакетов, логированием и интеграционными тестами через pytest.
  - `asyncio.DatagramProtocol` или аналог (решение из Эпика 0).
  - Получение пакетов, первичная валидация, логирование.
  - Интеграционные тесты с тестовым клиентом (pytest-asyncio).
- [x] **Задача 4.2. Обработка команд управления**
  - Комментарий: Добавлена обработка команд PROXY/HEARTBEAT/SERVERLIST/RELOAD в asyncio-демоне с проверкой ключа и тестами UDP.
  - Поддержка `PROXY ...`, `C;HEARTBEAT;`, `C;SERVERLIST;`, `C;RELOAD;`.
  - Проверка `proxy_key`.
  - Тесты на корректные и некорректные команды.
- [x] **Задача 4.3. Проксирование игровых пакетов**
  - Комментарий: Добавлена пересылка игровых UDP-пакетов с повторным назначением демонов при ошибках отправки и логированием проксируемого трафика.
  - Пересылка на выбранный демон, учёт состояния.
  - Ретраи при ошибках отправки.
  - Метрики по объёму трафика (через логирование/статистику).

## Эпик 5. Heartbeat и мониторинг
- [x] **Задача 5.1. Планировщик heartbeat**
  - Комментарий: Реализован HeartbeatManager с периодическим запуском целей, логированием ошибок и асинхронными тестами.
  - Периодические задачи, читают конфигурацию по частоте.
  - Отправка и ожидание ответов.
- [x] **Задача 5.2. Обработка ответов и смена статуса**
  - Комментарий: Добавлены UDP heartbeat-цели с обновлением статуса демонов, логированием переходов и записью в Proxy_Daemons.
  - Обновление `curstate`, `oldstate`, триггер перераспределения.
  - Нотификации (в логах/метриках) при переходах состояний.
- [x] **Задача 5.3. Механизм reload**
  - Комментарий: Реализована перезагрузка демонов из БД с обновлением балансировщика, heartbeat-целей и тестами UDP-команды reload.
  - Реакция на `C;RELOAD;` — обновление конфигурации БД/в памяти.
  - Тест: имитировать добавление/удаление демона.

## Эпик 6. Интеграция и тестирование
- [ ] **Задача 6.1. Юнит и интеграционные тесты**
  - Комментарий:
  - Покрыть парсер команд, балансировку, heartbeat.
  - Добиться порога покрытия (например, 80%).
- [ ] **Задача 6.2. E2E тестовый стенд**
  - Комментарий:
  - Docker-compose: MySQL + Python-демон + эмуляторы демонов.
  - Скрипты запуска и остановки.
- [ ] **Задача 6.3. Сравнительные тесты с Perl-версией**
  - Комментарий:
  - Сравнение логов и поведения на записи трафика.
  - Набор канонических пакетов и ожидаемых ответов.

## Эпик 7. Ввод в эксплуатацию
- [ ] **Задача 7.1. Документация и обучение**
  - Комментарий:
  - Руководство по развертыванию, настройке, мониторингу.
  - Обновление внутренних wiki/README.
- [ ] **Задача 7.2. Поэтапный запуск**
  - Комментарий:
  - Деплой Python-демона на staging.
  - Наблюдение в течение N дней, сбор метрик.
  - Переключение production с возможностью быстрого отката.
- [ ] **Задача 7.3. Пострелизный анализ**
  - Комментарий:
  - Сбор фидбэка от эксплуатации.
  - Добавление задач backlog по улучшениям.

## Эпик 8. Кросс-командные задачи
- [ ] **Задача 8.1. Безопасность и комплаенс**
  - Комментарий:
  - Проверка зависимостей на уязвимости (Snyk/Dependabot).
  - Обновление политики доступа к БД.
- [ ] **Задача 8.2. Наблюдаемость**
  - Комментарий:
  - Интеграция с существующим мониторингом (Prometheus/ELK).
  - Настройка алертов на heartbeat и ошибки БД.
- [ ] **Задача 8.3. План отката**
  - Комментарий:
  - Документ с инструкциями на возврат к Perl-версии.
  - Скрипты/анзибл плейбуки для быстрой смены.
